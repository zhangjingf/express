<!-- pages/order/index.wxml -->
<scroll-view scroll-y bindscrolltolower="lower">
  <view class="order" wx:for="{{orderList}}" wx:key="{{item.orderId}}" catchtap="goOrderDetail" data-id="{{item.orderId}}">
    <block wx:if="{{item.orderType == 0}}">
      <view class="part1">
        <view>寄快递</view>
        <view>{{item.createTime}}</view>
        <view>
          <view class="circle1"></view>
          {{item.receiverName}} {{item.receiverPhone}}
        </view>
        <view>{{item.receiverAddress}}</view>
        <view>
          <view class="circle2"></view>
          {{item.serviceTime}}
        </view>
      </view>
      <view class="part2">
        <view>
          <text wx:if="{{item.status == 0}}">待派单</text>
          <text wx:if="{{item.status == 10}}">派单中</text>
          <text wx:if="{{item.status == 20}}">已派单</text>
          <text wx:if="{{item.status == 30}}">已接单</text>
          <text wx:if="{{item.status == 40}}">已取件</text>
          <text wx:if="{{item.status == 50}}">配送中</text>
          <text wx:if="{{item.status == 60}}">已送达</text>
          <text wx:if="{{item.status == 70}}">用户取消</text>
          <text wx:if="{{item.status == 75}}">骑手取消</text>
          <text wx:if="{{item.status == 99}}">系统取消</text>
        </view>
        <view>
          {{item.expressName}}
          <text>{{item.orderNum}}</text>
        </view>
        <view>￥{{item.actualPrice}}</view>
        <view class="btns">
          <view bindtap="delete">删除</view>
          <view bindtap='goDetail'>查看物流</view>
        </view>
      </view>
    </block>
    <block wx:else>
      <view class="part1">
        <view>取快递</view>
        <view>{{item.createTime}}</view>
        <view>
          <view class="circle1"></view>
          {{item.expressName}}
        </view>
        <view>取货码：{{item.expressNumber}}</view>
        <view>
          <view class="circle2"></view>
          {{item.serviceTime}}
        </view>
      </view>
      <view class="part2">
        <view>
          <text wx:if="{{item.status == 0}}">待派单</text>
          <text wx:if="{{item.status == 10}}">派单中</text>
          <text wx:if="{{item.status == 20}}">已派单</text>
          <text wx:if="{{item.status == 30}}">已接单</text>
          <text wx:if="{{item.status == 40}}">已取件</text>
          <text wx:if="{{item.status == 50}}">配送中</text>
          <text wx:if="{{item.status == 60}}">已送达</text>
          <text wx:if="{{item.status == 70}}">用户取消</text>
          <text wx:if="{{item.status == 75}}">骑手取消</text>
          <text wx:if="{{item.status == 99}}">系统取消</text>
        </view>
        <view>
          <text>{{item.orderNum}}</text>
        </view>
        <view>￥{{item.actualPrice}}</view>
        <view class="btns">
          <view bindtap="delete">删除</view>
          <view bindtap='goDetail'>查看物流</view>
        </view>
      </view>
    </block>
  </view>
</scroll-view>
<nav checkedStatus="{{checkedStatus}}" />